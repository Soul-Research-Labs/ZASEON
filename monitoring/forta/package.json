{
  "name": "pil-forta-bots",
  "version": "1.0.0",
  "description": "Forta Detection Bots for PIL v2 Protocol Monitoring",
  "main": "dist/agent.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && forta-agent run",
    "test": "jest",
    "publish": "forta-agent publish",
    "push": "forta-agent push"
  },
  "dependencies": {
    "forta-agent": "^0.1.48",
    "ethers": "^6.9.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "jest": "^29.7.0",
    "typescript": "^5.3.3",
    "forta-agent-tools": "^3.2.6",
    "ts-jest": "^29.1.1"
  }
}
