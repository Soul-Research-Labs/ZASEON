[package]
name = "hekate_benchmark"
type = "bin"
version = "0.1.0"
authors = ["Soul Protocol"]
compiler_version = ">=0.33.0"
license = "MIT"
description = "Benchmark suite comparing Hekate-Groestl vs Poseidon for Noir circuits"

[dependencies]
hekate_hash = { path = "../hekate_hash" }
