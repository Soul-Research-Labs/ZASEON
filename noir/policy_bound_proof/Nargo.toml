[package]
name = "policy_bound_proof"
type = "bin"
authors = ["Privacy Interoperability Layer"]
compiler_version = ">=1.0.0"
description = "Policy-bound proofs cryptographically scoped by disclosure policy (PBP)"

[dependencies]
