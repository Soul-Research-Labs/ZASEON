[package]
name = "dilithium_proof"
type = "bin"
authors = ["Soul Protocol"]
compiler_version = ">=0.30.0"
description = "ZK proof of correct Dilithium (ML-DSA) signature verification"

[dependencies]
