[package]
name = "proof_carrying_container"
type = "bin"
authors = ["Privacy Interoperability Layer"]
compiler_version = ">=1.0.0"
description = "Self-authenticating confidential containers (PC³) circuit"

[dependencies]
