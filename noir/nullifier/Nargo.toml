[package]
name = "nullifier"
type = "bin"
authors = ["Soul Protocol"]
description = "Cross-Domain Nullifier Circuit (CDNA) - proves nullifier derivation for double-spend prevention"

[dependencies]
poseidon = { tag = "v0.2.3", git = "https://github.com/noir-lang/poseidon" }
