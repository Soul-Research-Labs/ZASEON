[package]
name = "hybrid_pqc_proof"
type = "bin"
authors = ["Soul Protocol"]
compiler_version = ">=0.30.0"
description = "ZK proof of correct hybrid (ECDSA + PQC) signature verification"

[dependencies]
