[package]
name = "cross_domain_nullifier"
type = "bin"
authors = ["Soul Protocol"]
description = "Domain-separated nullifiers (CDNA) that compose algebraically across chains"

[dependencies]
poseidon = { tag = "v0.2.3", git = "https://github.com/noir-lang/poseidon" }
